# platform-hono

NestJS HTTP adapter package for Hono.

This repository is only the initial package template. Adapter implementation,
tests, and package docs will be added in follow-up work.

## Commands

```bash
bun install
bun run check
bun run typecheck
bun run test
bun run test:integration
bun run build
```

